org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin's incremental compilation cannot close its caches under Flutter's
# shared plugin build directory on Windows, failing every plugin module with
# "Could not close incremental caches in ...caches-jvm". Full compilation costs
# a little time and builds reliably on every host.
kotlin.incremental=false
